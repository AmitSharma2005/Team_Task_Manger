{
  "name": "ethara-project",
  "version": "1.0.0",
  "description": "Ethara Team Task Manager",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "dev": "npx concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "puppeteer": "^25.0.2"
  }
}
